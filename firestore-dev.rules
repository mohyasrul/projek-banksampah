rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary rule untuk development - GANTI ini di production!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
